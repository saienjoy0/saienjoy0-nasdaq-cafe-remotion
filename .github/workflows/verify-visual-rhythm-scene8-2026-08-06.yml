name: Temporary Visual Rhythm Cleanup Target

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary cleanup target for the existing Preview verification workflow."
