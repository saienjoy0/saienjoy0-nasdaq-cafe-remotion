# Required Merge Gate docs-only verification

Disposable verification artifact for the 2026-09-02 bootstrap. This PR intentionally changes only `docs/**` and must receive `Nasdaq Cafe Required Merge Gate = success` without unrelated path-specific CI being required.
